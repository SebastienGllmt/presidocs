# dependencies (bun install)
node_modules

# output
out
dist
*.tgz
generated

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Cloudflare wrangler working directory
.wrangler

# Finder (MacOS) folder config
.DS_Store

# AI-authored post drafts (output of `bun run apply-comments`).
# Review with `diff` then `bun run promote-draft` to accept; the meta
# sidecar carries Claude's per-thread verdicts for auto-resolve on
# promote. Never committed directly.
posts/*.ai-draft.html
posts/*.ai-draft.meta.json
